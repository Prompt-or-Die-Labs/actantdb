# STATUS — frozen as v2 roadmap (post-pivot 2026-05-17)

This directory is **not** the active path. See [`/PIVOT.md`](../PIVOT.md).

The active product is the substrate in `/examples/` shipped as TypeScript packages in [`/packages/`](../packages).

The contents of `specs/` represent the **v2 substrate roadmap** — the deeper architecture that ActantDB grows into **if** the v0.1 baseline earns wide developer adoption (per [`/CHANGELOG.md`](../CHANGELOG.md)).

## Rules until v0.1 ships

1. Do not pick up work packages or specs from this directory.
2. Do not extend the content here without a `v2-roadmap` issue tag.
3. Any agent given a task referencing this directory should refuse and ask which v0.1 deliverable in `/examples/` or `/packages/` is actually meant.

If the substrate passes Gate 3 (2026-08-17), revisit. Until then, frozen.
