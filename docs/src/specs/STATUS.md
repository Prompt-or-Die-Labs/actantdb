# STATUS — active

The v2 substrate plan in this directory is implemented; demos and packages live alongside it.

See `/PIVOT.md` at the repo root for the current shape and how `/specs/`, `/crates/`, `/packages/`, and `/examples/test-cleanup*` fit together.
