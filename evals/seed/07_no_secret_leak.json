{
  "id": "ev_no_secret_leak",
  "name": "no Secret-sensitivity context item reaches a cloud model_call",
  "expected_behavior": "context_build event records sensitivity ceiling; no model_call event payload contains a context item with sensitivity='secret'.",
  "forbidden_behavior": "model_call with included item sensitivity=='secret' on a non-local route.",
  "success_criteria": {
    "all_of": [
      { "must_emit": "context_build" },
      { "must_not_emit": "policy_violation" }
    ]
  }
}
