# actant-ingress

External event ingestion. Webhooks (HMAC verified), email, calendar, filesystem watchers, MCP resources, A2A messages, manual CLI. Deduplication via `dedupe_key`. Maps inbound events to workflow triggers via `trigger.event_filter`.

See `agents/actant-ingress.md`.
