# actant-prompts

Prompt + tool-schema registry. Versioned prompt templates, renderers, eval harness, diff. Tool schemas are versioned alongside their input/output JSON Schema. Replay reads the exact prompt and tool-schema version a model saw.

See `agents/actant-prompts.md`.
