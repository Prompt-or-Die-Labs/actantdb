//! actant-cli library surface.
//!
//! actant-cli is primarily a binary (`src/main.rs`), but exposes a small
//! library surface for embedders and integration tests.
//!
//! Phase 1 scaffold. Implementation per the work package in
//! `agents/actant-cli.md`. Specs are in `/specs/`.

#![forbid(unsafe_code)]
