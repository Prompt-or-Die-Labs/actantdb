# actant-cache

Semantic + model-response + retrieval + reranker + tool-result + web-extraction + artifact-summary caches. Sensitivity-aware: cache policy travels with content; secret-class never cached, high-sensitivity local-only, cross-user reuse forbidden. Invalidates on memory/permission/policy changes.

See `agents/actant-cache.md`.
