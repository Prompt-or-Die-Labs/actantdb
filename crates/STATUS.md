# STATUS — active

Freeze lifted. The Cargo workspace has ~49 crates with real implementations (15k+ lines of Rust). `cargo metadata` is the source of truth for what's compiling.

See `/PIVOT.md` for how the crates map to the npm packages and the wedge demos.
