apiVersion: v1
kind: Service
metadata:
  name: {{ include "actantdb.fullname" . }}
  labels:
    {{- include "actantdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "actantdb.selectorLabels" . | nindent 4 }}
