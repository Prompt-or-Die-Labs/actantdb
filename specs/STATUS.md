# STATUS — active

The earlier freeze (2026-05-17 premortem-driven) was lifted. The v2 substrate plan in this directory is active in parallel with the v0.1 wedge — they grow together, not sequentially.

See `/PIVOT.md` at the repo root for the current shape and how `/specs/`, `/crates/`, `/packages/`, and `/wedge/demo*` fit together.
